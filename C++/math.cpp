#include <iostream>
#include <cmath>

int main(){

    double x;
    double y;
    double z;
    double max;
    double min;

    std::cout << "Enter a number of x: ";
    std::cin >> x;
    std::cout << "Enter a number of y: ";
    std::cin >> y;

    /////////// max function which will relate the grater value of two numbers /////////
    // max = std::max(x, y);
    // min = std::min(x, y);
///////////////// power function ///////////////
    // z = std::pow(x, y);
    // std::cout << "the power of " << x << " to " << y << " is " << z << std::endl;

//////////////// square root function /////////////////
    // z = sqrt(x);
    // std::cout << "the square root of " << x << " is " << z << std::endl;

///////////////// absolute value function ///////////////
    // z = abs(x);
    // std::cout << "the absolute value of " << x << " is " << z << std::endl;

/////////////////// round off function ///////////////////
    // z = round(x);
    // std::cout << "the rounded value of " << x << " is " << z << std::endl;

////////////////// round up function ////////////////////
    // z = ceil(x);
    // std::cout << "the rounded up value of " << x << " is " << z << std::endl;

/////////////////// round down function //////////////////
    // z = floor(x);
    // std::cout << "the rounded down value of " << x << " is " << z << std::endl;


/////////////////// grater and smaller with min and max /////////////////////////////
    // std::cout << "the grater number between " << x << " and " << y << " is " << max << std::endl;
    // std::cout << "the smaller number between " << x << " and " << y << " is " << min << std::endl;





    return 0;
}
